name: winners_world_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  gap:
  get:
  firebase_auth: ^4.6.1
  firebase_core: ^2.13.0
  firebase_database:
  http: ^0.13.6
  shared_preferences: ^2.1.1
  cloud_firestore: ^4.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  

flutter:
  uses-material-design: true
  

  assets:
    - img/
